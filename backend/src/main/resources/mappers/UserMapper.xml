<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.chillandcode.model.dao.UserDao">
	<!-- 회원 가입(일반 등록) -->
	<insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
		INSERT INTO users (email, password, nickname, region, provider, provider_id)
		VALUES (#{email}, #{password}, #{nickname}, #{region}, #{provider}, #{providerId})
	</insert>
	
	<!-- 회원 가입(OAuth) -->
	<insert id="insertOAuthUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
		INSERT INTO users (email, provider, provider_id, password, nickname)
		VALUES (#{email}, #{provider}, #{providerId}, #{password}, #{nickname})
	</insert>
	
	<!-- 회원 정보 조회 (프로필 조회) -->
	<select id="selectUser" parameterType="Long" resultType="User">
		SELECT *
		FROM users
		WHERE user_id = #{userId}
			AND is_deleted = 0
	</select>
	
	<!-- 회원 정보 수정 (프로필 수정) -->
	<update id="updateUser" parameterType="UserUpdateRequest">
		UPDATE users
		<set>
			<if test="password != null">
				password = #{password},
			</if>
			<if test="nickname != null">
				nickname = #{nickname},
			</if>
			<if test="region != null">
				region = #{region},
			</if>
		</set>
		WHERE user_id=#{userId}
			AND is_deleted = 0
	</update>
	
	<!-- 회원 정보 삭제 (탈퇴) -->
	<update id="softDelete" parameterType="Long">
		UPDATE users
		SET 
			is_deleted = 1,
			deleted_at = NOW()
		WHERE user_id = #{userId}
	</update>
	
	<!--이메일 중복 검사-->
	<select id="existsByEmail" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM users
		WHERE email = #{email}
	</select>
	
	<!--회원 단건 조회 (일반 로그인)-->
	<select id="findByEmail" parameterType="String" resultType="User">
		SELECT *
		FROM users
		WHERE email = #{email}
			AND provider IS NULL
	</select>
	
	<!--회원 단건 조회 (OAuth) -->
	<select id="findByProvider" parameterType="map" resultType="User">
		SELECT *
		FROM users
		WHERE provider = #{provider}
			AND provider_id = #{providerId}
	</select>
	
</mapper>
